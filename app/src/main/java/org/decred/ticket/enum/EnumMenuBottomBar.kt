package org.decred.ticket.enum

import org.decred.ticket.R

enum class EnumMenuBottomBar(val type: Int) {
    SETTINGS(R.string.settings),
    EXTRACT(R.string.extract),
    BALANCE(R.string.balance)
}
